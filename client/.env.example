# API Configuration
VITE_API_URL=http://localhost:5002

# ⚠️ SECURITY WARNING ⚠️
# NEVER use VITE_ prefix for sensitive credentials!
# Variables prefixed with VITE_ are bundled into client-side JavaScript
# and are visible to anyone inspecting the built application in browser dev tools.
#
# For demo/development mode, implement authentication server-side:
# - Create a demo login endpoint that validates credentials server-side
# - Return a JWT/session token to the client
# - Never expose credentials in environment variables
#
# The following are COMMENTED OUT to prevent accidental exposure:
# VITE_DEMO_ADMIN_PASSWORD=admin123  # ❌ DO NOT UNCOMMENT - SECURITY RISK
# VITE_DEMO_ADMIN_USERNAME=admin     # ❌ DO NOT UNCOMMENT - SECURITY RISK
